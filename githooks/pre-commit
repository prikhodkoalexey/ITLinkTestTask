#!/bin/sh
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
SWIFTLINT_SCRIPT="${REPO_ROOT}/Scripts/run-swiftlint.sh"

if [ ! -x "${SWIFTLINT_SCRIPT}" ]; then
  echo "[pre-commit] Не найден скрипт SwiftLint: ${SWIFTLINT_SCRIPT}" >&2
  exit 1
fi

"${SWIFTLINT_SCRIPT}" >&2
